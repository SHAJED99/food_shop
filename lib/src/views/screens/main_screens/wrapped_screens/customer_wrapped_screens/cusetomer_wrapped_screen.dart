import 'package:flutter/material.dart';

class CustomerWrappedScreen extends StatelessWidget {
  const CustomerWrappedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
